# Copy this to .env and fill in your values

# Modal credentials (set via CLI: modal token set --token-id X --token-secret Y)
MODAL_TOKEN_ID=your-token-id-here
MODAL_TOKEN_SECRET=your-token-secret-here

# Set true to use Modal cloud for hand tracking (faster)
USE_MODAL=false
